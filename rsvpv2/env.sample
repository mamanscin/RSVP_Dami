# Database connection string for Prisma (SQLite by default).
# In Docker Compose, this is set via docker-compose.yml instead.
# DATABASE_URL=file:./prisma/dev.db

# Admin password to access /admin section.
# If not set, the admin section is disabled.
# ADMIN_PASSWORD=changeme
